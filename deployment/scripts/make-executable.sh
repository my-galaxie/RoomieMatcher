#!/bin/bash

# Make all deployment scripts executable
chmod +x deploy.sh
chmod +x cleanup.sh

echo "All deployment scripts are now executable!"
echo "You can now run ./deploy.sh with appropriate parameters to start deployment." 